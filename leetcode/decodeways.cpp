#include <iostream>
using namespace std;

class Solution {
public:
    int numDecodings(string s) {
        return 0; 
    }
};

int main()
{
    string s = "";
    Solution so;
    int result;
    result = so.numDecodings(s);
    cout<<result<<endl;
}